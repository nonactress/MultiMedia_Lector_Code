# DP_Assignment2_Clean 알고리즘 로직 흐름 분석

**파일:** `DP_Assignment2_Clean.cpp`  
**작성일:** 2026-04-02  
**주요 기능:** 영상의 경계 검출을 통한 관심 영역(ROI) 추출

---

## 1. 밝기 분석을 통한 기본 특성 파악

### 목적
영상의 밝기 정보를 축(X축, Y축) 단위로 분석하여 이미지의 전체적인 밝기 분포를 파악합니다.

### 구현 방식

**X축 밝기 분석 (stage_calculateBrightnessX):**

각 열(column)에 대해 밝기의 평균값을 계산합니다. 이를 위해 원본 이미지를 행 방향(Y축)으로 순회하며 각 픽셀의 RGB 값을 더한 후 채널 수(3)로 나누어 그레이스케일 값을 구합니다. 각 열별로 계산된 모든 그레이스케일 값들을 누적하여 이미지의 높이로 나눔으로써 해당 열의 평균 밝기값(avgX)을 도출합니다.

```
과정: for x in [0, width):
        for y in [0, height):
            밝기 = (R + G + B) / 3
            누적값 += 밝기
        avgX[x] = 누적값 / height
```

**Y축 밝기 분석 (stage_calculateBrightnessY):**

각 행(row)에 대해 밝기의 평균값을 계산합니다. X축 분석과 동일한 원리이지만 순회 방향이 반대입니다. 각 행별로 모든 픽셀의 그레이스케일 값을 더한 후 이미지의 너비로 나눔으로써 해당 행의 평균 밝기값(avgY)을 도출합니다.

```
과정: for y in [0, height):
        for x in [0, width):
            밝기 = (R + G + B) / 3
            누적값 += 밝기
        avgY[y] = 누적값 / width
```

### 의의
X축과 Y축의 평균 밝기 배열은 이미지의 거시적(macro-level) 밝기 분포를 표현하며, 이후 미분 연산을 통해 경계 변화를 감지하는 기초 데이터가 됩니다.

---

## 2. 표준편차 계산을 통한 영역 특성 분석

### 목적
각 행과 열의 밝기 변동성(분산 정도)을 계산하여 영상 내 특정 영역의 균일성을 파악합니다. 낮은 표준편차는 해당 영역이 균일한 밝기를 가짐을 의미하며, 높은 표준편차는 다양한 밝기를 가짐을 의미합니다.

### 구현 방식

**X축 표준편차 계산 (stage_calculateVarianceX):**

각 열별로 그 열 내 모든 픽셀의 밝기가 열의 평균 밝기(avgX[x])로부터 얼마나 떨어져 있는지를 측정합니다. 각 픽셀의 밝기에서 평균을 뺀 값을 제곱한 후 모두 더하고, 이를 픽셀 개수로 나눈 후 제곱근을 취합니다.

```
과정: for x in [0, width):
        누적값 = 0
        for y in [0, height):
            밝기 = (R + G + B) / 3
            차이 = 밝기 - avgX[x]
            누적값 += 차이²
        stdX[x] = √(누적값 / height)
```

**Y축 표준편차 계산 (stage_calculateVarianceY):**

각 행별로 그 행 내 모든 픽셀의 밝기가 행의 평균 밝기(avgY[y])로부터 얼마나 떨어져 있는지를 측정합니다. X축과 동일한 원리를 행 단위로 적용합니다.

```
과정: for y in [0, height):
        누적값 = 0
        for x in [0, width):
            밝기 = (R + G + B) / 3
            차이 = 밝기 - avgY[y]
            누적값 += 차이²
        stdY[y] = √(누적값 / width)
```

### 의의
표준편차는 밝기의 불규칙성을 정량화합니다. 특히 Y축 표준편차(stdY)는 각 행의 세부 정보량을 나타내며, 경계 영역(검은색 테두리)의 낮은 표준편차와 콘텐츠 영역의 높은 표준편차 간 차이가 경계 검출의 핵심 신호가 됩니다.

---

## 3. 표준편차 미분을 통한 경계 변화 감지

### 목적
표준편차 값이 급격히 변하는 위치를 찾아냅니다. 표준편차의 미분값이 크다는 것은 해당 위치가 영역의 특성이 급격히 바뀌는 지점, 즉 경계임을 의미합니다.

### 구현 방식

**1차 미분 계산 (stage_calculateStddevDiff):**

각 행의 표준편차에서 인접한 행의 표준편차를 뺍니다. 이는 표준편차가 어떤 방향으로 변하는지를 나타냅니다.

```
과정: for y in [0, height-1):
        stddevDiff[y] = stdY[y+1] - stdY[y]
```

- **양수 값:** 다음 행으로 갈수록 표준편차가 증가 (균일한 영역에서 복잡한 영역으로 진입)
- **음수 값:** 다음 행으로 갈수록 표준편차가 감소 (복잡한 영역에서 균일한 영역으로 진입)
- **절대값이 큼:** 경계의 가능성이 높음

### 의의
표준편차 1차 미분은 경계의 후보 위치를 더 명확하게 제시합니다. 특히 음수에서 양수로 변하거나 양수에서 음수로 변하는 부분이 경계입니다.

---

## 4. 표준편차 2차 미분을 통한 경계 집중화

### 목적
1차 미분의 급격한 변화를 감지하여 경계가 가장 명확한 위치를 찾습니다. 2차 미분은 곡률(curvature)을 나타내며, 절대값이 클수록 변화가 가파릅니다.

### 구현 방식

**2차 미분 계산 (stage_calculateStddevDiff2):**

1차 미분값들 간의 차이를 다시 계산합니다. 이는 미분값 자체가 얼마나 빠르게 변하는지를 나타냅니다.

```
과정: for y in [0, height-2):
        stddevDiff2[y] = stddevDiff[y+1] - stddevDiff[y]
```

- **절대값이 큼:** 미분값이 급격히 변함 = 경계 근처
- **영점 교차(0 부근):** 경계의 정확한 위치

### 의의
표준편차 2차 미분을 이용함으로써 다음 두 가지 장점을 얻습니다:

1. **노이즈 감소:** 점진적인 밝기 변화는 2차 미분에서 작은 값으로 나타나므로, 경계라는 급격한 변화를 부각시킵니다.
2. **경계 위치 정확화:** 1차 미분이 최댓값 또는 최솟값에 도달하는 지점이 바로 경계의 중심입니다.

---

## 5. 상위 12개 경계 검출 및 필터링

### 목적
표준편차 2차 미분 배열에서 절대값이 가장 큰 12개의 위치를 찾되, 너무 가깝게 몰려있는 경계들을 제거합니다.

### 구현 방식

**Step 1: 절대값 기준 상위 12개 위치 선택**

표준편차 2차 미분 배열을 순회하며 각 위치의 절대값이 현재까지의 상위 12개 중 하나보다 크면 해당 위치를 기록합니다. 이 과정에서 기존 값들을 한 칸씩 뒤로 밀어냅니다.

```
과정: for y in [0, sizeY-2):
        absVal = |stddevDiff2[y]|
        if absVal > 상위12 중 가장 작은 값:
            해당 위치를 올바른 순서로 삽입
```

**Step 2: Y축 순서로 재정렬**

선택된 12개의 위치를 Y축 좌표 순서로 정렬합니다(버블 정렬 사용). 이는 경계들을 위에서 아래로 순서대로 나열하기 위함입니다.

```
과정: 버블정렬(tempPositions, Y축 기준)
```

**Step 3: 근접한 경계 필터링**

정렬된 경계들 중에서 인접한 두 경계 간의 거리가 LIMITS_NEARBY(설정값: 5)보다 작으면 절대값이 작은 경계를 제거합니다. 이는 미세한 노이즈로 인한 다중 검출을 방지합니다.

```
과정: for i in [0, 12):
        if i > 0 and |position[i] - position[i-1]| < LIMITS_NEARBY:
            position[i]를 제거
```

**Step 4: 최종 결과 배열에 복사**

필터링 후 남은 경계들(최대 12개, 최소 0개)을 최종 boundaries 배열에 복사합니다. 12개 미만이면 나머지 위치는 -1로 채웁니다.

### 의의
이 단계를 통해 다음을 달성합니다:

1. **신호 강도 순선택:** 가장 명확한 경계 12개를 자동으로 선택
2. **노이즈 제거:** 가까운 거리에 여러 경계가 감지된 경우 하나만 유지
3. **구조화된 출력:** Y축 순서로 정렬된 경계 리스트 제공

---

## 6. 1차 미분을 통한 기본 경계 검출

### 목적
밝기값의 1차 미분을 계산하여 밝기가 급격히 변하는 위치를 찾습니다. 이는 모서리와 경계의 기본적인 감지 방법입니다.

### 구현 방식

**1차 미분 계산 (stage_calculateFirstDerivative):**

인접한 두 지점의 밝기 차이를 계산합니다.

```
X축: diffX[x] = avgX[x+1] - avgX[x]
Y축: diffY[y] = avgY[y+1] - avgY[y]
```

- **양수 값:** 밝기가 증가하는 구간
- **음수 값:** 밝기가 감소하는 구간
- **절대값이 큼:** 급격한 밝기 변화 = 경계의 가능성

### 의의
1차 미분은 단순하지만 강력한 경계 검출 방법입니다. 다만 노이즈에 민감하므로, 실제 경계 검출에서는 표준편차 기반의 2차 미분(Step 4에서 계산)을 우선적으로 사용합니다.

---

## 7. 최종 경계 좌표 결정

### 목적
1차 미분값에서 최댓값과 최솟값의 위치를 찾아 최종 경계 좌표(leftX, rightX, topY, bottomY)를 결정합니다.

### 구현 방식

**경계 검출 (stage_findBoundary):**

1차 미분 배열을 순회하며:

- **최솟값(음수 피크):** 밝기가 급격히 감소하는 지점 = 좌측 경계 또는 상측 경계
- **최댓값(양수 피크):** 밝기가 급격히 증가하는 지점 = 우측 경계 또는 하측 경계

```
과정: for i in [0, size):
        if diff[i] < minVal:
            minVal = diff[i]
            minPos = i
        if diff[i] > maxVal:
            maxVal = diff[i]
            maxPos = i
```

**결과:**
- X축에 적용: leftX(최솟값), rightX(최댓값)
- Y축에 적용: topY(최솟값), bottomY(최댓값)

### 의의
이 최종 단계에서 4개의 경계 좌표가 확정되어 관심 영역(ROI: Region of Interest)이 정의됩니다.

---

## 8. 시각화 및 결과 출력

### 목적
각 처리 단계의 중간 결과와 최종 결과를 시각적으로 표현하여 알고리즘의 동작을 검증합니다.

### 생성되는 시각화

1. **Brightness X/Y 그래프:** 평균 밝기 분포를 선 그래프로 표현
2. **Standard Deviation Y 그래프:** 각 행의 표준편차를 선 그래프로 표현
3. **StdDev Diff Y 그래프:** 표준편차 1차 미분을 빨강(음수)/초록(양수)으로 구분
4. **StdDev Diff2 Y 그래프:** 표준편차 2차 미분을 빨강(음수)/초록(양수)으로 구분
5. **Detected Boundaries 그래프:** 검출된 경계를 원본 이미지 위에 선으로 표시
6. **ROI 박스:** 최종 경계로 정의된 관심 영역을 초록색 사각형으로 표시

### 의의
시각화를 통해:

- 알고리즘의 각 단계가 올바르게 작동하는지 확인
- 경계 검출의 정확도를 육안으로 평가
- 파라미터 조정의 필요성 판단

---

## 요약: 알고리즘의 논리적 흐름

```
원본 이미지
    ↓
[단계 1] 밝기 분석 (평균값 계산)
    ↓ (avgX, avgY)
[단계 2] 표준편차 계산 (변동성 분석)
    ↓ (stdX, stdY)
[단계 3] 표준편차 1차 미분
    ↓ (stddevDiff)
[단계 4] 표준편차 2차 미분
    ↓ (stddevDiff2)
[단계 5] 상위 12개 경계 선택 및 필터링
    ↓ (boundaries[12])
[단계 6] 1차 미분 계산 (보조 경계 검출)
    ↓ (diffX, diffY)
[단계 7] 최종 경계 좌표 결정
    ↓ (leftX, rightX, topY, bottomY)
[단계 8] 시각화 및 결과 출력
    ↓
ROI 추출 및 표시
```

---

## 핵심 통찰

### 왜 표준편차를 사용하는가?

1. **영역 특성의 정량화:** 각 행/열의 밝기 변동성을 수치화
2. **노이즈 강건성:** 평균값 기반 분석보다 국소 특성을 잘 반영
3. **경계의 명확한 신호:** 균일한 영역(낮은 표준편차)과 복잡한 영역(높은 표준편차) 간의 명확한 구분

### 왜 2차 미분을 사용하는가?

1. **경계 강조:** 급격한 변화(곡률)를 부각
2. **노이즈 감소:** 점진적 변화는 약한 신호로 나타남
3. **위치 정확화:** 최댓값/최솟값이 경계의 정확한 위치를 나타냄

### 알고리즘의 장점

- ✅ 복잡한 영상에서도 경계를 안정적으로 검출
- ✅ 조명 변화에 상대적으로 강건
- ✅ 다단계 필터링으로 노이즈 제거
- ✅ 시각적 검증이 용이

### 알고리즘의 한계

- ❌ 경계가 불명확한 영상에서는 성능 저하
- ❌ 계산량이 많음 (각 픽셀을 여러 번 순회)
- ❌ 파라미터(LIMITS_NEARBY)의 수동 조정 필요
