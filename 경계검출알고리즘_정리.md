# 영상 경계 검출 알고리즘 상세 분석

## 1. 밝기 분석을 통한 기본 특성 파악

영상의 밝기 정보를 축 단위로 분석하여 이미지의 전체적인 밝기 분포를 파악하기 위해 X축과 Y축 방향의 밝기 분석을 수행하였다. X축 밝기 분석에서는 각 열에 대해 밝기의 평균값을 계산한다. 이를 위해 원본 이미지를 행 방향으로 순회하며 각 픽셀의 RGB 값을 더한 후 채널 수인 3으로 나누어 그레이스케일 값을 구한다. 각 열별로 계산된 모든 그레이스케일 값들을 누적하여 이미지의 높이로 나눔으로써 해당 열의 평균 밝기값을 도출한다. Y축 밝기 분석도 동일한 원리이지만 순회 방향이 반대이다. 각 행에 대해 모든 픽셀의 그레이스케일 값을 더한 후 이미지의 너비로 나눔으로써 해당 행의 평균 밝기값을 도출한다. 이렇게 계산된 X축과 Y축의 평균 밝기 배열은 이미지의 거시적 밝기 분포를 표현하며, 이후 미분 연산을 통해 경계 변화를 감지하는 기초 데이터가 된다.

## 2. 표준편차 계산을 통한 영역 특성 분석

각 행과 열의 밝기 변동성을 계산하여 영상 내 특정 영역의 균일성을 파악한다. 낮은 표준편차는 해당 영역이 균일한 밝기를 가짐을 의미하며, 높은 표준편차는 다양한 밝기를 가짐을 의미한다. X축 표준편차 계산에서는 각 열별로 그 열 내 모든 픽셀의 밝기가 열의 평균 밝기로부터 얼마나 떨어져 있는지를 측정한다. 각 픽셀의 밝기에서 평균을 뺀 값을 제곱한 후 모두 더하고, 이를 픽셀 개수로 나눈 후 제곱근을 취함으로써 표준편차를 계산한다. Y축 표준편차 계산도 각 행별로 동일한 원리를 적용하여 해당 행 내 모든 픽셀의 밝기 변동성을 계산한다. 표준편차는 밝기의 불규칙성을 정량화하며, 특히 Y축 표준편차는 각 행의 세부 정보량을 나타내므로 경계 영역의 낮은 표준편차와 콘텐츠 영역의 높은 표준편차 간 차이가 경계 검출의 핵심 신호가 된다.

## 3. 표준편차 미분을 통한 경계 변화 감지

표준편차 값이 급격히 변하는 위치를 찾기 위해 1차 미분을 수행한다. 1차 미분은 각 행의 표준편차에서 인접한 행의 표준편차를 빼는 방식으로 계산되며, 이는 표준편차가 어떤 방향으로 변하는지를 나타낸다. 양수 값은 다음 행으로 갈수록 표준편차가 증가하는 것을 의미하며, 이는 균일한 영역에서 복잡한 영역으로 진입함을 의미한다. 음수 값은 다음 행으로 갈수록 표준편차가 감소하는 것을 의미하며, 이는 복잡한 영역에서 균일한 영역으로 진입함을 의미한다. 절대값이 클수록 경계의 가능성이 높다. 표준편차 1차 미분은 경계의 후보 위치를 더 명확하게 제시한다.

## 4. 표준편차 2차 미분을 통한 경계 집중화

1차 미분의 급격한 변화를 감지하여 경계가 가장 명확한 위치를 찾기 위해 2차 미분을 수행한다. 2차 미분은 곡률을 나타내며, 절대값이 클수록 변화가 가파르다. 2차 미분은 1차 미분값들 간의 차이를 계산함으로써 미분값 자체가 얼마나 빠르게 변하는지를 나타낸다. 절대값이 큰 2차 미분은 미분값이 급격히 변함을 의미하며 경계 근처에 위치함을 나타낸다. 영점 교차 부근은 경계의 정확한 위치를 나타낸다. 표준편차 2차 미분을 이용함으로써 노이즈 감소와 경계 위치의 정확화를 달성할 수 있다. 점진적인 밝기 변화는 2차 미분에서 작은 값으로 나타나므로 경계라는 급격한 변화를 부각시킬 수 있으며, 1차 미분이 최댓값 또는 최솟값에 도달하는 지점이 바로 경계의 중심이 된다.

## 5. 상위 12개 경계 검출 및 필터링

표준편차 2차 미분 배열에서 절대값이 가장 큰 12개의 위치를 찾되, 너무 가깝게 몰려있는 경계들을 제거한다. 먼저 표준편차 2차 미분 배열을 순회하며 각 위치의 절대값이 현재까지의 상위 12개 중 하나보다 크면 해당 위치를 기록하고 기존 값들을 한 칸씩 뒤로 밀어낸다. 그 다음 선택된 12개의 위치를 Y축 좌표 순서로 정렬한다. 이는 경계들을 위에서 아래로 순서대로 나열하기 위함이다. 정렬된 경계들 중에서 인접한 두 경계 간의 거리가 설정된 임계값(LIMITS_NEARBY)보다 작으면 절대값이 작은 경계를 제거한다. 이는 미세한 노이즈로 인한 다중 검출을 방지하기 위함이다. 필터링 후 남은 경계들을 최종 경계 배열에 복사하며, 12개 미만이면 나머지 위치는 -1로 채운다. 이 단계를 통해 가장 명확한 경계를 자동으로 선택하고 노이즈를 제거하며 구조화된 출력을 제공할 수 있다.

## 6. 1차 미분을 통한 기본 경계 검출

밝기값의 1차 미분을 계산하여 밝기가 급격히 변하는 위치를 찾는다. X축에서는 인접한 두 열의 평균 밝기 차이를 계산하고, Y축에서는 인접한 두 행의 평균 밝기 차이를 계산한다. 양수 값은 밝기가 증가하는 구간을 의미하고, 음수 값은 밝기가 감소하는 구간을 의미한다. 절대값이 크면 급격한 밝기 변화를 의미하므로 경계의 가능성이 높다. 1차 미분은 단순하지만 강력한 경계 검출 방법이지만 노이즈에 민감하므로, 실제 경계 검출에서는 표준편차 기반의 2차 미분을 우선적으로 사용한다.

## 7. 최종 경계 좌표 결정

1차 미분값에서 최댓값과 최솟값의 위치를 찾아 최종 경계 좌표를 결정한다. 1차 미분 배열을 순회하면서 최솟값의 위치를 찾는데, 이는 밝기가 급격히 감소하는 지점을 의미하며 좌측 경계 또는 상측 경계에 해당한다. 최댓값의 위치는 밝기가 급격히 증가하는 지점을 의미하며 우측 경계 또는 하측 경계에 해당한다. X축의 1차 미분에 적용하면 좌측(leftX, 최솟값) 및 우측(rightX, 최댓값) 경계 좌표를 구할 수 있으며, Y축의 1차 미분에 적용하면 상측(topY, 최솟값) 및 하측(bottomY, 최댓값) 경계 좌표를 구할 수 있다. 이 최종 단계에서 4개의 경계 좌표가 확정되어 관심 영역(ROI: Region of Interest)이 정의된다.

## 8. 시각화 및 결과 출력

각 처리 단계의 중간 결과와 최종 결과를 시각적으로 표현하여 알고리즘의 동작을 검증한다. Brightness X/Y 그래프는 평균 밝기 분포를 선 그래프로 표현하며, Standard Deviation Y 그래프는 각 행의 표준편차를 선 그래프로 표현한다. StdDev Diff Y 그래프는 표준편차 1차 미분을 음수는 빨강색, 양수는 초록색으로 구분하여 표현하고, StdDev Diff2 Y 그래프는 표준편차 2차 미분을 동일하게 표현한다. Detected Boundaries 그래프는 검출된 경계를 원본 이미지 위에 선으로 표시하며, ROI 박스는 최종 경계로 정의된 관심 영역을 초록색 사각형으로 표시한다. 시각화를 통해 알고리즘의 각 단계가 올바르게 작동하는지 확인할 수 있으며, 경계 검출의 정확도를 육안으로 평가할 수 있고, 파라미터 조정의 필요성을 판단할 수 있다.

## 알고리즘의 핵심 특성

표준편차를 경계 검출에 사용하는 이유는 다음과 같다. 첫째, 각 행과 열의 밝기 변동성을 수치화하여 영역 특성을 정량화할 수 있다. 둘째, 평균값 기반 분석보다 국소 특성을 잘 반영하여 노이즈에 강건하다. 셋째, 균일한 영역과 복잡한 영역 간의 명확한 구분이 경계의 명확한 신호가 된다. 2차 미분을 사용하는 이유는 다음과 같다. 첫째, 급격한 변화인 곡률을 부각하여 경계를 강조한다. 둘째, 점진적 변화는 약한 신호로 나타나므로 노이즈를 감소시킨다. 셋째, 최댓값과 최솟값이 경계의 정확한 위치를 나타낸다. 본 알고리즘의 장점은 복잡한 영상에서도 경계를 안정적으로 검출할 수 있으며, 조명 변화에 상대적으로 강건하고, 다단계 필터링으로 노이즈를 제거하며, 시각적 검증이 용이하다는 점이다. 반면 한계점은 경계가 불명확한 영상에서는 성능이 저하될 수 있으며, 각 픽셀을 여러 번 순회하므로 계산량이 많고, 파라미터의 수동 조정이 필요하다는 점이다.
